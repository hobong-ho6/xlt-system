# XLT System 번역 워크플로우 가이드

## 개요

XLT System은 피그마 디자인과 엑셀 파일의 한국어 텍스트를 다국어(영어, 일본어, 중국어 번체, 태국어)로 자동 번역하는 시스템입니다. 내부적으로 Claude AI를 활용하여 높은 품질의 번역을 제공합니다.

---

## 1. 엑셀 번역 워크플로우

### 📊 **전체 프로세스 개요**
```
사용자 파일 업로드 → 파일 검증 → 텍스트 분석 → 맞춤법 교정 → 용어집 적용 → Claude AI 번역 → 결과 저장 → 다운로드 제공
```

### 🔄 **상세 단계별 처리**

#### **1단계: 파일 수신 및 검증 (20%)**
**코드 위치:** `stable_web_server.py` → `api_excel_translate()` → `process_excel_translation_from_content()`

**처리 내용:**
- 사용자가 업로드한 엑셀 파일을 메모리로 로드
- 파일 형식 검증 (`.xlsx`, `.xls` 지원)
- 필수 워크시트 'properties' 존재 확인
- 파일 손상 여부 체크

**시스템 로그 예시:**
```
📁 엑셀 번역 시작: sample.xlsx (엔진: claude)
📊 워크시트 목록: ['properties', 'metadata']
```

#### **2단계: 워크시트 구조 분석 (40%)**
**코드 위치:** `process_excel_translation_from_content()` → 헤더 분석 섹션

**처리 내용:**
- 1행의 컬럼 헤더 분석 및 매핑
- 한국어 컬럼 자동 감지 (`ko_KR`, `ko`, `Korean`, `한국어`, `원본`, `Original`)
- 대상 언어 컬럼 확인 (`en_US`, `ja_JP`, `zh_TW`, `th_TH`)
- 누락된 언어 컬럼 자동 생성

**시스템 로그 예시:**
```
컬럼 1: ko_KR
컬럼 2: en_US
한국어 컬럼 발견: ko_KR (컬럼 1)
새 ja_JP 컬럼 생성: 컬럼 5
```

#### **3단계: 번역 대상 텍스트 수집 (60%)**
**코드 위치:** `process_excel_translation_from_content()` → 번역 태스크 수집 섹션

**처리 내용:**
- 2행부터 모든 데이터 스캔
- 한글 포함 텍스트 필터링 (정규식 `[가-힣]` 사용)
- 이미 번역된 항목 제외 (빈 셀만 번역 대상)
- 번역 태스크 객체 생성 및 큐잉

**시스템 로그 예시:**
```
🔤 엑셀 번역 대상 분석 완료: 8개 한글 텍스트 발견
```

#### **4단계: Claude AI 통합 번역 (80%)**
**코드 위치:** `translate_with_claude_integrated()` → `xlt/translation/claude_translator.py`

**세부 처리 과정:**

##### **4-1. 텍스트 전처리**
- **맞춤법 교정:** 한국어 원문의 띄어쓰기 및 맞춤법 자동 교정
- **용어집 적용:** LINE API에서 로드한 25개 전문 용어 매칭 및 적용
- **배치 최적화:** 10개씩 묶어서 처리 효율성 향상

##### **4-2. Claude AI 번역 실행**
```python
# 실제 Claude CLI 명령 실행
claude_command = ['claude']
result = subprocess.run(claude_command, input=prompt, capture_output=True, text=True, timeout=120)
```

**Claude AI 프롬프트 구조:**
```
번역 지침:
1. 한국어 텍스트의 맞춤법을 교정하세요
2. 전문 용어집을 참조하여 일관성 있는 번역을 제공하세요
3. 각 언어별 문화적 맥락을 고려하세요
4. JSON 형식으로 결과를 반환하세요

텍스트: "사용자 계정 설정"
대상 언어: en_US, ja_JP, zh_TW, th_TH
```

##### **4-3. 청크 기반 병렬 처리**
**코드 위치:** `translate_with_claude_integrated()` → 청크 처리 섹션

- **청크 분할:** 전체 텍스트를 10개씩 묶어서 처리
- **병렬 실행:** 각 청크를 독립적으로 Claude AI에 전송
- **결과 병합:** 모든 청크 결과를 최종 번역 결과로 통합

**시스템 로그 예시:**
```
📊 엑셀 청크 기반 처리 시작: 35개 텍스트 → 10개씩 분할
대상 언어: ja_JP, zh_TW, th_TH, en_US
✅ 엑셀 청크 1/4 완료: 청크 1/4 완료 (10개 텍스트, 21.5초)
🎉 엑셀 Claude 청크 번역 완료: 35/35 성공 (성공률: 100.0%)
```

#### **5단계: 결과 적용 및 파일 생성 (100%)**
**코드 위치:** `process_excel_translation_from_content()` → 결과 적용 섹션

**처리 내용:**
- 번역 결과를 해당 셀에 자동 입력
- 타임스탬프가 포함된 파일명 생성
- 완성된 엑셀 파일을 서버에 저장
- 다운로드 링크 생성 및 반환

**시스템 로그 예시:**
```
💾 엑셀 번역 완료: 140개 텍스트 → translated_20260519_150223_sample.xlsx
```

---

## 2. 피그마 번역 워크플로우

### 🎨 **전체 프로세스 개요**
```
피그마 URL 입력 → 디자인 접근 → 이미지 다운로드 → 텍스트 추출 → 맞춤법 교정 → 사용자 선택 → Claude AI 번역 → 엑셀 생성
```

### 🔄 **상세 단계별 처리**

#### **1단계: 피그마 URL 검증 및 접근**
**코드 위치:** `stable_web_server.py` → `figma_preview()` 함수

**처리 내용:**
- URL 형식 검증 (`figma.com` 도메인 확인)
- 피그마 토큰을 통한 API 인증
- 디자인 파일 접근 권한 확인
- 파일 메타데이터 수집

#### **2단계: 디자인 이미지 다운로드**
**코드 위치:** `xlt/input/figma_processor.py` → 이미지 다운로드 섹션

**처리 내용:**
- 피그마 API를 통한 고해상도 PNG 이미지 다운로드
- 서버 로컬 저장 (`figma/session_[ID]_figma.png`)
- 웹페이지 미리보기용 이미지 URL 생성

**시스템 로그 예시:**
```
📸 피그마 이미지 다운로드 중 (미리보기용)...
✅ 미리보기용 이미지 저장: session_20260519_163417_aee79a26_figma.png
🔗 이미지 URL 설정: /figma_image/session_20260519_163417_aee79a26_figma.png
```

#### **3단계: 텍스트 직접 추출**
**코드 위치:** `figma_processor.extract_text_from_node()` 함수

**처리 내용:**
- 피그마 API의 텍스트 노드 직접 접근
- 디자인 내 모든 텍스트 레이어 스캔
- 텍스트 속성 정보 수집 (폰트, 크기, 위치 등)
- 한글 텍스트 필터링 및 수집

**시스템 로그 예시:**
```
🎨 피그마에서 텍스트 직접 추출 중...
✅ 피그마에서 63개 텍스트 추출 완료
💡 텍스트 노드에서 직접 가져왔습니다 (정확도 100%)
```

#### **4단계: 텍스트 전처리 및 교정**
**코드 위치:** `claude_translator.py` → 맞춤법 교정 섹션

**처리 내용:**
- **자동 맞춤법 교정:** Claude AI를 통한 한국어 문법 교정
- **용어 통일:** 전문 용어집 기반 용어 표준화
- **텍스트 정제:** 불필요한 공백 및 특수문자 제거

**시스템 로그 예시:**
```
🔤 교정: 'USDT는 디지털달러 처럼...' → 'USDT는 디지털 달러처럼...'
🔤 교정: '기간한정' → '기간 한정'
✅ 텍스트 추출 완료: 63개 텍스트 발견 (교정 적용됨)
```

#### **5단계: 사용자 텍스트 선택 인터페이스**
**코드 위치:** `static/js/app.js` → 피그마 텍스트 선택 UI

**처리 내용:**
- 추출된 텍스트 목록을 웹페이지에 표시
- 체크박스를 통한 개별 텍스트 선택 기능
- 전체 선택/해제 토글 기능
- 선택된 텍스트만 번역 대상으로 설정

#### **6단계: Claude AI 번역 실행**
**코드 위치:** `claude_translator.translate_quality_focused()` 함수

**고품질 번역 프로세스:**
- **1차 번역:** 원문을 각 언어로 기본 번역
- **2차 검증:** 번역 품질 점수 자동 평가
- **3차 개선:** 낮은 점수 항목 재번역
- **최종 검수:** 용어집 일관성 재확인

#### **7단계: 엑셀 파일 생성 및 다운로드**
**코드 위치:** `create_excel_with_custom_keys()` 함수

**처리 내용:**
- XLT 표준 포맷 엑셀 파일 생성
- `properties` 워크시트에 번역 결과 입력
- 메타데이터 워크시트 생성 (번역 정보, 타임스탬프 등)
- 다운로드 가능한 파일로 저장

---

## 3. 핵심 기술 구성요소

### 🤖 **Claude AI 번역 엔진**
**파일 위치:** `xlt/translation/claude_translator.py`

**주요 기능:**
- **캐시 시스템:** 동일 텍스트 재번역 방지 (2시간 TTL)
- **배치 처리:** 최대 10개 텍스트 동시 번역
- **품질 평가:** 자동 품질 점수 산출 및 개선
- **타임아웃 관리:** 120초 안전 제한

### 📚 **용어집 관리 시스템**
**데이터 소스:** LINE API 연동 (25개 전문 용어)

**처리 방식:**
- 번역 전 용어집 매칭 및 적용
- 다국어 용어 일관성 보장
- 실시간 용어집 업데이트 지원

### 💾 **세션 관리 및 진행 상태 추적**
**구현 위치:** `session_status` 전역 딕셔너리

**추적 정보:**
- 실시간 진행률 (% 및 단계별 메시지)
- 처리 시간 측정 및 예상 완료 시간
- 오류 발생 시 상세 로그 기록

---

## 4. 품질 보장 시스템

### ✅ **다중 검증 레이어**
1. **입력 검증:** 파일 형식 및 구조 확인
2. **텍스트 검증:** 한글 포함 여부 및 번역 필요성 체크
3. **번역 품질:** Claude AI 자체 품질 평가 시스템
4. **결과 검증:** 번역 완료 항목 수 및 일관성 확인

### 🔄 **자동 복구 시스템**
- **캐시 미스:** 동일 텍스트 재요청 시 캐시 활용
- **API 타임아웃:** 120초 제한으로 무한 대기 방지
- **청크 재시도:** 실패한 청크만 재처리
- **세션 복구:** 중단된 번역 작업 이어서 진행

---

## 5. 성능 최적화

### ⚡ **병렬 처리**
- **청크 분할:** 대용량 텍스트를 10개씩 분할하여 처리
- **독립 실행:** 각 청크가 별도 Claude AI 세션에서 처리
- **결과 병합:** 모든 청크 완료 후 최종 결과 통합

### 💾 **캐싱 전략**
- **번역 캐시:** 동일 텍스트 재번역 방지 (2시간 유지)
- **LRU 정책:** 오래된 캐시 자동 제거 (최대 2000개 항목)
- **캐시 통계:** 히트율 및 메모리 사용량 모니터링

---

## 6. 시스템 아키텍처 다이어그램

### 엑셀 번역 플로우
```
[사용자] 
    ↓ 엑셀 업로드
[Flask 웹서버] 
    ↓ 파일 검증
[Excel Processor] 
    ↓ 구조 분석
[Text Collector] 
    ↓ 한글 텍스트 수집
[Claude Translator] 
    ↓ 청크별 번역
[Result Merger] 
    ↓ 결과 통합
[Excel Generator] 
    ↓ 파일 생성
[다운로드 제공]
```

### 피그마 번역 플로우
```
[사용자] 
    ↓ 피그마 URL 입력
[Figma API] 
    ↓ 디자인 접근
[Image Downloader] 
    ↓ 이미지 다운로드
[Text Extractor] 
    ↓ 텍스트 추출
[Text Corrector] 
    ↓ 맞춤법 교정
[User Interface] 
    ↓ 텍스트 선택
[Claude Translator] 
    ↓ 고품질 번역
[Excel Generator] 
    ↓ 결과 파일 생성
[다운로드 제공]
```

---

## 7. API 엔드포인트

### 엑셀 번역 관련
- `POST /api/excel-translate` - 엑셀 파일 번역 시작
- `GET /api/excel-progress/<session_id>` - 번역 진행 상태 조회
- `GET /api/download-excel/<filename>` - 번역된 파일 다운로드

### 피그마 번역 관련
- `POST /api/figma-preview` - 피그마 URL 미리보기
- `POST /api/translate` - 번역 실행
- `GET /api/status/<session_id>` - 세션 상태 조회

### 설정 관리
- `POST /api/settings/test-figma-token` - 피그마 토큰 테스트
- `GET /api/health` - 시스템 상태 확인

---

이 워크플로우를 통해 XLT System은 높은 품질과 일관성을 가진 다국어 번역 서비스를 제공하며, 사용자는 간단한 파일 업로드나 URL 입력만으로 전문적인 번역 결과를 얻을 수 있습니다.

## 8. 로그 및 모니터링

### 시스템 로그 레벨
- **INFO:** 일반적인 처리 과정 기록
- **SUCCESS:** 성공적인 작업 완료
- **WARNING:** 주의가 필요한 상황 (토큰 누락 등)
- **ERROR:** 오류 발생 상황
- **DEBUG:** 개발 및 디버깅용 상세 정보

### 로그 파일 위치
- **서버 로그:** `/logs/server.log` (10MB 로테이션, 3개 백업)
- **콘솔 출력:** 실시간 상태 모니터링

### 주요 모니터링 지표
- **번역 성공률:** 청크별 성공/실패 비율
- **처리 시간:** 청크당 평균 처리 시간
- **캐시 히트율:** 중복 번역 요청 최적화 정도
- **메모리 사용량:** 캐시 및 세션 데이터 사용량